

define([], function () {

    var formatDash = function () {
        var retVal = "";


        return retVal;

    };

    var utility = {
        
        FormatDate: function(dateVal,format) {
            var dateString  = dataVal;
            switch (format.toUpperCase()) {
                case "MM-DD-YY":
                case "m-DD-YY":

                    break;
            }
            return dateString;
        }

    };


    return utility;
});

